export const ANSWER_TYPES : { SIMPLE: string, MULTIPLE: string, FREE: string } = {
    SIMPLE: "simple",
    MULTIPLE: "multiple",
    FREE: "free"
}

export const ROLES : { TEACHER: string, STUDENT: string } = {
    TEACHER: "teacher",
    STUDENT: "student"
}